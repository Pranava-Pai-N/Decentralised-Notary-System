### Backend setup (FastAPI)

```bash
cd backend
python -m venv venv
./venv/Scripts/activate
pip install -r requirements.txt
```

### Frontend Setup (NextJS)
```bash
cd frontend
npm install
npm run dev
```